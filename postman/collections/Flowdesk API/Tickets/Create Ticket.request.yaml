$kind: http-request
name: Create Ticket
method: POST
url: '{{baseUrl}}/tickets'
order: 1000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "title": "Sample ticket title",
      "description": "Sample ticket description"
    }
